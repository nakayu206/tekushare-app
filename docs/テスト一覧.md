# テスト一覧

単体テスト（`test/`）と結合テスト（`integration_test/`）の全テストケースを一覧化したドキュメント。

実行コマンド: `xvfb-run flutter test -d linux --reporter expanded`

---

## 単体テスト（208件）

### コア層

#### `test/core/config/flavor_test.dart` — AppConfig（3件）

| # | テスト名 |
|---|---------|
| 1 | デフォルトのflavorはdev |
| 2 | stg環境に切り替えるとflavorとappNameが変わる |
| 3 | prod環境に切り替えるとflavorとappNameが変わる |

#### `test/firebase_options_test.dart` — DefaultFirebaseOptions（3件）

| # | テスト名 |
|---|---------|
| 4 | Androidオプションに正しいprojectIdとmessagingSenderIdが設定されている |
| 5 | iOSオプションに正しいprojectIdとmessagingSenderIdが設定されている |
| 6 | Webオプションに正しいprojectIdとmessagingSenderIdが設定されている |

---

### ドメイン層

#### `test/domain/entities/lat_lng_test.dart` — LatLng（3件）

| # | テスト名 |
|---|---------|
| 7 | 同じ座標は等しい |
| 8 | 異なる座標は等しくない |
| 9 | 同じ座標は hashCode が一致する |

#### `test/domain/entities/spot_test.dart` — Spot（8件）

| # | テスト名 |
|---|---------|
| 10 | 初期値が正しく設定される |
| 11 | markAsVisited でステータスが visited になる |
| 12 | markAsWantToGo でステータスが wantToGo になる |
| 13 | copyWith でタイトルを変更できる |
| 14 | copyWith でメモと写真パスを設定できる |
| 15 | copyWith で memo を null に戻せる |
| 16 | copyWith で photoPath を null に戻せる |
| 17 | copyWith で memo を指定しないと元の値を保持する |

#### `test/domain/entities/walk_route_test.dart` — WalkRoute（4件）

| # | テスト名 |
|---|---------|
| 18 | 初期値が正しく設定される |
| 19 | copyWith でポイントリストを更新できる |
| 20 | copyWith で変更しないフィールドは元の値を保持する |
| 21 | points は外部から変更できない |

#### `test/domain/entities/walk_session_test.dart` — WalkSession（6件）

| # | テスト名 |
|---|---------|
| 22 | デフォルト状態は idle で経過秒数 0 |
| 23 | copyWith でステータスを walking に変更できる |
| 24 | copyWith でステータスを finished に変更できる |
| 25 | copyWith で変更しないフィールドは元の値を保持する |
| 26 | copyWith で startedAt を null に戻せる |
| 27 | copyWith で finishedAt を null に戻せる |

#### `test/domain/usecases/photo/attach_photo_to_spot_test.dart` — AttachPhotoToSpot（2件）

| # | テスト名 |
|---|---------|
| 28 | attachPhoto が正しい引数で呼ばれる |
| 29 | attachPhoto が1回だけ呼ばれる |

#### `test/domain/usecases/spot/get_spots_test.dart` — GetSpots（4件）

| # | テスト名 |
|---|---------|
| 30 | filter なしで全スポットを返す |
| 31 | wantToGo フィルタで wantToGo のみ返す |
| 32 | visited フィルタで visited のみ返す |
| 33 | 一致するスポットがなければ空リストを返す |

#### `test/domain/usecases/spot/save_spot_test.dart` — SaveSpot（5件）

| # | テスト名 |
|---|---------|
| 34 | saveSpot が1回呼ばれる |
| 35 | 渡したタイトル・座標でスポットが保存される |
| 36 | デフォルトのステータスは wantToGo |
| 37 | status を visited で指定できる |
| 38 | 生成したスポットIDを返す |

#### `test/domain/usecases/spot/update_spot_status_test.dart` — UpdateSpotStatus（2件）

| # | テスト名 |
|---|---------|
| 39 | updateSpotStatus が正しい引数で呼ばれる |
| 40 | wantToGo ステータスでも呼び出せる |

#### `test/domain/usecases/walk/check_inactivity_test.dart` — CheckInactivity（3件）

| # | テスト名 |
|---|---------|
| 41 | 10分を超えていれば true を返す |
| 42 | 10分以内であれば false を返す |
| 43 | 9分59秒では false を返す |

#### `test/domain/usecases/walk/end_walk_test.dart` — EndWalk（6件）

| # | テスト名 |
|---|---------|
| 44 | finished ステータスのセッションを返す |
| 45 | finishedAt が設定される |
| 46 | elapsedSeconds が startedAt からの経過秒数になる |
| 47 | startedAt が null なら elapsedSeconds は 0 |
| 48 | saveSession が呼ばれる |
| 49 | saveRoute が呼ばれる |

#### `test/domain/usecases/walk/get_walk_history_test.dart` — GetWalkHistory（3件）

| # | テスト名 |
|---|---------|
| 50 | getAllSessions の結果をそのまま返す |
| 51 | セッションが0件のときは空リストを返す |
| 52 | getAllSessions が1回呼ばれる |

#### `test/domain/usecases/walk/start_walk_test.dart` — StartWalk（5件）

| # | テスト名 |
|---|---------|
| 53 | walking ステータスのセッションを返す |
| 54 | startedAt が現在時刻付近にセットされる |
| 55 | id が空でない |
| 56 | elapsedSeconds の初期値が 0 |
| 57 | finishedAt は null |

---

### データ層

#### `test/data/models/spot_model_test.dart` — SpotModel（4件）

| # | テスト名 |
|---|---------|
| 58 | fromEntity でエンティティからモデルに変換できる |
| 59 | toEntity でモデルからエンティティに変換できる |
| 60 | memo / photoPath が null のまま変換できる |
| 61 | visited ステータスが正しく変換される |

#### `test/data/models/walk_route_model_test.dart` — WalkRouteModel（4件）

| # | テスト名 |
|---|---------|
| 62 | fromEntity でエンティティからモデルに変換できる |
| 63 | toEntity でモデルからエンティティに変換できる |
| 64 | ポイントリストが正しく変換される |
| 65 | 空のポイントリストが変換できる |

#### `test/data/models/walk_session_model_test.dart` — WalkSessionModel（3件）

| # | テスト名 |
|---|---------|
| 66 | fromEntity で idle セッションを変換できる |
| 67 | fromEntity で finished セッションを変換できる |
| 68 | toEntity でモデルからエンティティに変換できる |

#### `test/data/repositories/photo_repository_impl_test.dart` — PhotoRepositoryImpl（3件）

| # | テスト名 |
|---|---------|
| 69 | attachPhoto でスポットに画像パスを紐づけできる |
| 70 | attachPhoto で存在しない spotId を指定しても例外にならない |
| 71 | attachPhoto で画像パスを上書きできる |

#### `test/data/repositories/route_repository_impl_test.dart` — RouteRepositoryImpl（4件）

| # | テスト名 |
|---|---------|
| 72 | saveRoute で保存したルートを getAllRoutes で取得できる |
| 73 | getRouteBySessionId でセッションIDに対応するルートを取得できる |
| 74 | getRouteBySessionId で存在しない sessionId は null を返す |
| 75 | saveRoute は同じ id で upsert される |

#### `test/data/repositories/spot_repository_impl_test.dart` — SpotRepositoryImpl（5件）

| # | テスト名 |
|---|---------|
| 76 | saveSpot で保存したスポットを getSpots で取得できる |
| 77 | saveSpot は同じ id で upsert される |
| 78 | updateSpotStatus でステータスを変更できる |
| 79 | updateSpotStatus で存在しない id を指定しても例外にならない |
| 80 | deleteSpot でスポットを削除できる |

#### `test/data/repositories/walk_session_repository_impl_test.dart` — WalkSessionRepositoryImpl（4件）

| # | テスト名 |
|---|---------|
| 81 | saveSession で保存したセッションを getAllSessions で取得できる |
| 82 | getSessionById で id に対応するセッションを取得できる |
| 83 | getSessionById で存在しない id は null を返す |
| 84 | saveSession は同じ id で upsert される |

---

### インフラストラクチャ層

#### `test/infrastructure/camera_service_test.dart` — CameraService（4件）

| # | テスト名 |
|---|---------|
| 85 | takePhoto: 撮影した画像のパスを返す |
| 86 | takePhoto: キャンセル時は null を返す |
| 87 | pickFromGallery: 選択した画像のパスを返す |
| 88 | pickFromGallery: キャンセル時は null を返す |

#### `test/infrastructure/location_service_test.dart` — LocationService（6件）

| # | テスト名 |
|---|---------|
| 89 | 位置情報サービスが無効な場合は例外をスローする |
| 90 | パーミッションが拒否された場合は例外をスローする |
| 91 | unableToDetermine の場合は requestPermission を呼び許可されれば通過する |
| 92 | unableToDetermine で requestPermission も拒否された場合は例外をスローする |
| 93 | パーミッションが永久拒否された場合は例外をスローする |
| 94 | パーミッションが許可されている場合は現在地を返す |

#### `test/infrastructure/notification_service_test.dart` — NotificationService（4件）

| # | テスト名 |
|---|---------|
| 95 | initialize で plugin.initialize が呼ばれる |
| 96 | showInactivityNotification で id=1 の通知が表示される |
| 97 | showTurnaroundNotification で id=2 の通知が表示される |
| 98 | cancelAll で plugin.cancelAll が呼ばれる |

---

### プレゼンテーション層 — プロバイダー

#### `test/screens/providers/auth_provider_test.dart` — EmailAuthNotifier / DisplayNameNotifier（8件）

| # | グループ | テスト名 |
|---|---------|---------|
| 99 | EmailAuthNotifier | 初期状態は EmailAuthIdle |
| 100 | EmailAuthNotifier | register 成功時はエラー状態にならない |
| 101 | EmailAuthNotifier | register でメールが重複すると EmailAuthError に遷移する |
| 102 | EmailAuthNotifier | signIn 成功時はエラー状態にならない |
| 103 | EmailAuthNotifier | signIn でパスワードが間違うと EmailAuthError に遷移する |
| 104 | EmailAuthNotifier | reset を呼ぶと EmailAuthIdle に戻る |
| 105 | DisplayNameNotifier | save 成功時は AsyncData(null) になる |
| 106 | DisplayNameNotifier | save 失敗時は AsyncError になる |

#### `test/screens/providers/inactivity_provider_test.dart` — InactivityNotifier（2件）

| # | テスト名 |
|---|---------|
| 107 | 初期状態は現在時刻付近 |
| 108 | updateLastAction で lastActionAt が更新される |

#### `test/screens/providers/spot_provider_test.dart` — SpotNotifier / filteredSpotsProvider（7件）

| # | グループ | テスト名 |
|---|---------|---------|
| 109 | SpotNotifier | 初期状態でリポジトリから spots を購読する |
| 110 | SpotNotifier | saveSpot で saveSpot が呼ばれる |
| 111 | SpotNotifier | updateStatus で updateSpotStatus が呼ばれる |
| 112 | SpotNotifier | attachPhoto で attachPhoto が呼ばれる |
| 113 | filteredSpotsProvider | filter なしで全件返す |
| 114 | filteredSpotsProvider | wantToGo フィルタで wantToGo のみ返す |
| 115 | filteredSpotsProvider | visited フィルタで visited のみ返す |

#### `test/screens/providers/walk_session_provider_test.dart` — WalkSessionNotifier（6件）

| # | テスト名 |
|---|---------|
| 116 | 初期状態は idle |
| 117 | startWalk で walking 状態になる |
| 118 | endWalk で finished 状態になる |
| 119 | endWalk で saveSession が呼ばれる |
| 120 | endWalk で saveRoute が呼ばれる |
| 121 | idle 状態で endWalk を呼んでもリポジトリが呼ばれない |

---

### プレゼンテーション層 — ウィジェット

#### `test/screens/widgets/common/app_bottom_nav_test.dart` — AppBottomNav（2件）

| # | テスト名 |
|---|---------|
| 122 | sets selected index correctly |
| 123 | calls onTap when tapped |

#### `test/screens/widgets/common/category_chip_group_test.dart` — CategoryChipGroup（3件）

| # | テスト名 |
|---|---------|
| 124 | shows six categories |
| 125 | calls onSelected when a category chip is tapped |
| 126 | reflects selectedCategory change in display |

#### `test/screens/widgets/common/dashed_border_painter_test.dart` — DashedBorderPainter（1件）

| # | テスト名 |
|---|---------|
| 127 | shouldRepaint は常に false を返す |

#### `test/screens/widgets/common/primary_button_test.dart` — PrimaryButton（2件）

| # | テスト名 |
|---|---------|
| 128 | shows label correctly |
| 129 | calls onPressed when tapped |

---

### プレゼンテーション層 — 画面

#### `test/widget_test.dart` — アプリルート（1件）

| # | テスト名 |
|---|---------|
| 130 | 未認証時はメール認証画面を表示する |

#### `test/screens/pages/home/home_page_test.dart` — HomePage 旧（2件）

| # | テスト名 |
|---|---------|
| 131 | navigates to WalkPage when start walk button is tapped |
| 132 | navigates to SpotListPage when bottom nav list is tapped |

#### `test/screens/pages/home/view/home_page_test.dart` — HomePage（4件）

| # | テスト名 |
|---|---------|
| 133 | 初期状態でホーム画面が表示される |
| 134 | 散歩を始めるボタン押下でセッションが walking 状態になる |
| 135 | walking 状態になったら WalkPage へ遷移する |
| 136 | walking 状態のまま戻って再度ボタンを押しても WalkPage へ遷移する |

#### `test/screens/pages/walk/walk_page_test.dart` — WalkPage（9件）

| # | テスト名 |
|---|---------|
| 137 | shows three buttons |
| 138 | shows bottom navigation |
| 139 | shows loading indicator while GPS is acquiring |
| 140 | shows error text when GPS is unavailable |
| 141 | shows snackbar when camera tapped without GPS |
| 142 | stores photo path in pendingPhotoProvider when photo taken |
| 143 | shows photoTaken snackbar after photo is taken |
| 144 | does not store photo when camera is cancelled |
| 145 | navigates to EndWalkPage when end walk button is tapped |

#### `test/screens/pages/walk/end_walk_page_test.dart` — EndWalkPage（6件）

| # | テスト名 |
|---|---------|
| 146 | shows end walk confirm message |
| 147 | shows cancel button |
| 148 | shows confirm button |
| 149 | shows bottom navigation |
| 150 | tapping cancel goes back to previous screen |
| 151 | tapping confirm goes back to home screen |

#### `test/screens/pages/map/walk_route_page_test.dart` — WalkRoutePage（10件）

| # | テスト名 |
|---|---------|
| 152 | shows page title |
| 153 | shows saved routes section |
| 154 | shows route list items |
| 155 | shows selected route section |
| 156 | shows bottom navigation |
| 157 | tapping a route changes selection |
| 158 | shows save confirmation dialog when showSaveDialogOnLoad is true |
| 159 | canceling save dialog closes dialog |
| 160 | confirming save shows saved dialog |
| 161 | closing saved dialog closes dialog |

#### `test/screens/pages/spot/want_to_go_page_test.dart` — WantToGoPage（8件）

| # | テスト名 |
|---|---------|
| 162 | shows page title |
| 163 | confirmation dialog shows no-title placeholder when title is empty |
| 164 | confirmation dialog shows entered title |
| 165 | canceling confirmation dialog closes dialog |
| 166 | confirming save shows save complete dialog |
| 167 | closing save complete dialog leaves the page |
| 168 | tapping a category chip changes selection state |
| 169 | pressing save button shows confirmation dialog |

#### `test/screens/pages/spot/spot_list_page_test.dart` — SpotListPage（9件）

| # | テスト名 |
|---|---------|
| 170 | shows page title |
| 171 | shows two tabs |
| 172 | tapping went tab switches to went list |
| 173 | shows six category chips |
| 174 | tapping a category chip changes selection state |
| 175 | shows bottom navigation |
| 176 | shows back button in AppBar |
| 177 | can switch back to want-to-go tab from went tab |
| 178 | tapping bottom nav home goes back to previous screen |
| 179 | tapping a list item navigates to SpotDetailPage |
| 180 | tapping an item in went tab navigates to SpotDetailPage in went mode |

#### `test/screens/pages/spot/spot_detail_page_test.dart` — SpotDetailPage（20件）

| # | テスト名 |
|---|---------|
| 181 | shows want-to-go title in want-to-go mode |
| 182 | shows went title in went mode |
| 183 | shows six category chips |
| 184 | shows two add photo areas |
| 185 | shows delete button |
| 186 | shows save button |
| 187 | shows bottom navigation |
| 188 | tapping a category chip toggles selection color |
| 189 | tapping bottom nav list goes back to previous screen |
| 190 | tapping bottom nav home goes back to first screen |
| 191 | delete confirmation dialog shows entered title |
| 192 | pressing delete button shows delete confirmation dialog |
| 193 | canceling delete confirmation closes dialog |
| 194 | confirming delete shows deletion complete dialog |
| 195 | closing deletion complete dialog leaves the page |
| 196 | pressing save button shows save confirmation dialog |
| 197 | save confirmation dialog shows entered title |
| 198 | canceling save confirmation closes dialog |
| 199 | confirming save shows save complete dialog |
| 200 | closing save complete dialog leaves the page |

---

## 結合テスト（2件）

#### `integration_test/app_test.dart` — app launch flow（2件）

| # | テスト名 |
|---|---------|
| 201 | launches app and shows home screen |
| 202 | navigates to list tab via bottom nav |

---

## テスト数まとめ

| 分類 | 件数 |
|-----|-----|
| コア層 | 6 |
| ドメイン層（エンティティ） | 21 |
| ドメイン層（ユースケース） | 23 |
| データ層（モデル） | 11 |
| データ層（リポジトリ） | 16 |
| インフラストラクチャ層 | 14 |
| プレゼンテーション層（プロバイダー） | 23 |
| プレゼンテーション層（ウィジェット） | 8 |
| プレゼンテーション層（画面） | 86 |
| **単体テスト 合計** | **208** |
| 結合テスト | 2 |
| **総計** | **210** |
